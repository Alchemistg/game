(function initUiEvents(global) {
  "use strict";

  // Refactor scaffold:
  // bindInventoryEvents, bindTradeEvents, bindContextEvents, bindMenuEvents.
  global.UIEvents = global.UIEvents || {
    version: 1
  };
})(window);

