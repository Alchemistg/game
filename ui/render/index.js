(function initUiRender(global) {
  "use strict";

  // Refactor scaffold:
  // renderBoard, renderPlayers, renderInventory, renderContextMenu, renderTrade.
  global.UIRender = global.UIRender || {
    version: 1
  };
})(window);

