(function initCoreState(global) {
  "use strict";

  // Refactor scaffold:
  // This module will host state shape, selectors, markPlayerDirty,
  // and save/load serialization helpers.
  global.CoreState = global.CoreState || {
    version: 1
  };
})(window);

